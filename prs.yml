# List of upstream helix-editor/helix PRs to merge into the `nightly` branch.
# Order matters: PRs are merged top-to-bottom on top of upstream/master.
#
# Required field:
#   number:   the upstream PR number
#
# Optional fields:
#   note:     free-form text — useful to remember what each PR is about
#   pin:      <sha> — pin to a specific commit instead of the latest PR head
#   enabled:  false — temporarily skip without deleting the entry

prs:
  - number: 8675
    note: "replace upstream PR title here"
